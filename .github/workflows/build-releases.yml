name: Build & Release Lyrune

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller -r requirements.txt

      - name: Build Executable with PyInstaller (Folder & Single-File Standalone)
        run: |
          pyinstaller --noconfirm --onedir --windowed --name "Lyrune" main.py
          pyinstaller --noconfirm --onefile --windowed --name "Lyrune-Standalone" main.py

      - name: Compress Release Archive
        run: |
          Compress-Archive -Path "dist/Lyrune/*" -DestinationPath "Lyrune-Windows-x64.zip"
          Copy-Item "dist/Lyrune-Standalone.exe" -Destination "Lyrune-Standalone-x64.exe"

      - name: Upload Windows Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Lyrune-Windows-x64
          path: Lyrune-Windows-x64.zip

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libdbus-1-dev libglib2.0-dev

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller -r requirements.txt

      - name: Build Linux Binary with PyInstaller
        run: |
          pyinstaller --noconfirm --onedir --windowed --name "Lyrune" main.py

      - name: Archive Linux Release
        run: |
          tar -czvf Lyrune-Linux-x64.tar.gz -C dist/Lyrune .

      - name: Upload Linux Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Lyrune-Linux-x64
          path: Lyrune-Linux-x64.tar.gz

  release:
    needs: [build-windows, build-linux]
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    steps:
      - name: Download Windows Artifact
        uses: actions/download-artifact@v4
        with:
          name: Lyrune-Windows-x64

      - name: Download Linux Artifact
        uses: actions/download-artifact@v4
        with:
          name: Lyrune-Linux-x64

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            Lyrune-Standalone-x64.exe
            Lyrune-Windows-x64.zip
            Lyrune-Linux-x64.tar.gz
